-- Function syntax
myFunc :: (Integral x) => x -> x -> x

{-
    myFunc The name
    (Integral x) The range of values on which the function acts
    x -> x -> x This takes 2 arguments of Integral type and returns one Integral 
-}